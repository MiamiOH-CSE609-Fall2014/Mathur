#include<stdio.h>
#include<stdlib.h>
#include<vector>
#include<iostream>
#include<string>


using namespace std;

int main()
{int a;
  vector<int> input;
  int rm1;
  int cm1;
  int rm2;
  int cm2;
  string s;
  
while(getline(cin,s))
    { //cout<<s<<endl;
      string m=s;
      a=atoi(m.c_str());
      input.push_back(a);
    }

  for(int i=0;i<input.size();i++)
    cout<<input[i]<<endl;
  return 0;
}
